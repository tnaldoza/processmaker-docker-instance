# Docker Compose Setup for ProcessMaker v4

## TODO

- Update setup to point to `./processmaker` previous setup was inside `./processmaker` root instead of project root.
- Add LICENSE
- Unable to login, redirectrs to [http://localhost:8080](http://localhost:8080)
